[project]
name = "reaction-dynamics"
version = "0.2.0"
description = "Turns Slack reaction timing into team clarity"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]
live = ["slack_sdk>=3.43"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["seed", "tests"]
addopts = "-v"
